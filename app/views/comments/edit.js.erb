var comment_div = $("#<%= dom_id @comment %>");
comment_div.slideToggle("fast", function () {
    comment_div.html("<%= escape_javascript(render "comments/form", :comment => @comment) %>").hide().slideToggle("fast");
});
