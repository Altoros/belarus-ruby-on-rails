<hr/>
<%= form_for comment, :remote => true do |f| %>
  <div><%= f.text_area :body %></div>
  <div><%= f.hidden_field :custom_news_id %></div>
  <div><%= f.hidden_field :user_id %></div>
  <div><%= f.submit "Leave a comment" %></div>
<% end %>