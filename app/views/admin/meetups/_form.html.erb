  <%= form_for [:admin, @meetup] do |f| %>
    <table class="loginTBL">
        <tr>
            <td><%= f.label :topic, t('meetup.topic') %></td>
            <td>
                <div class="field"><%= f.text_field :topic %></div>
            </td>
        </tr>
        <tr>
            <td><%= f.label :description, t('meetup.description') %></td>
            <td>
                <div class="field"><%= f.text_area :description %></div>
            </td>
        </tr>
        <tr>
            <td><%= f.label :place, t('meetup.place') %></td>
            <td>
                <div class="field"><%= f.text_field :place %></div>
            </td>
        </tr>
        <tr>
            <td><%= f.label "date_and_time", t('meetup.date_and_time') %></td>
            <td>
                <div class="field">
                  <%= f.text_field :date_and_time,  :value => ( @meetup.date_and_time.present? ? @meetup.date_and_time.strftime('%Y-%m-%d %H:%M:%S') : '') %>
                </div>
            </td>
        </tr>
        <tr>
            <td></td>
            <td class="save">
              <%= content_tag :button, :type => :submit, :name => :commit do %>
                <span><span><%= t('meetup.save_button') %></span></span>
              <% end %>
              <%= link_to t('meetup.back_link'), admin_meetups_path %>
            </td>
        </tr>
    </table>
  <% end %>

  <% content_for :head do %>
      <% javascript_tag do %>
        $(document).ready(function(){
            $('#meetup_date_and_time').datetimepicker({
                minDate: 0,
                dateFormat: 'yy-mm-dd',
                timeFormat: 'hh:mm:ss'
            })
        })
      <% end %>
  <% end %>
